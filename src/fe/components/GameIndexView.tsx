import * as React from "react";
import {Col, Avatar, Row, Button, Card} from 'antd';

export interface IndexViewProps {

}

class GameIndexView extends React.Component<IndexViewProps, {}> {
    render() {

        return (
            <div/>
        );
    }
}

export default GameIndexView;
